Eugenia Cattelan 17908/4

Para ejecutar el programa del ejercicio 10 es necsario tener instaladas las librerias que figuran en el requirements.txt:
    - notebook==7.3.3
    - pandas==2.2.3
Para esto es posible instalarlas utilizando el comando "pip install -r requirements.txt"

La version de pyhton que utiliza el entorno vitual es 3.12.9

El archivo de jupyter main.ipynb en el cual se debe ejecutar el programa se ejecuto en VSCode. Para que funcione correctamente hay que reinciar el kernel de jupyter y luego ejecutar si o si la primer linea. 
El programa fue probado y se subio al repositorio virtual con las celdas de salida inclusive